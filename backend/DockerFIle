# Gunakan image Python yang ringan
FROM python:3.10-slim

# Set folder kerja di dalam container
WORKDIR /app

# Copy requirements.txt dulu biar caching layer docker efisien
COPY requirements.txt .

# Install dependencies
# (Tambahkan build-essential jika ada error compile library tertentu)
RUN apt-get update && apt-get install -y build-essential && \
    pip install --no-cache-dir -r requirements.txt

# Copy seluruh kode backend ke dalam container
COPY . .

# Expose port 8000 (Port FastAPI)
EXPOSE 8000

# Jalankan server uvicorn
# Host 0.0.0.0 PENTING agar bisa diakses dari luar container
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]